name: _disabled_legacy_django_ci

on:
  workflow_dispatch:  # disabled legacy workflow; kept for reference only

jobs:
  lint:
    if: false  # disabled
    runs-on: ubuntu-latest
    steps:
      - run: echo "Legacy workflow disabled; see ci.yml"
